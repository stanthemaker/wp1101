{
	"name": "final",
	"version": "1.0.0",
	"main": "index.js",
	"license": "MIT",
	"scripts": {
		"start": "cd backend && yarn && yarn server",
		"heroku-postbuild": "cd frontend && yarn && yarn build",
		"server": "cd backend && yarn && yarn server",
		"frontend": "cd frontend && yarn start",
		"backend": "cd backend && yarn server"
	},
	"dependencies": {
		"axios": "^0.24.0"
	}
}
